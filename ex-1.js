function sortCustomerName(customers) {
 const result = "ตอนนี้ผมยังปวดหัวกับโปรเจคกลุ่มที่ทำวันนี้อยู่ยังคิดไรไม่ออก ก๊อปโจทย์"
                      "ไว้เสาทิตย์จะไปนั่งทำความเข้าใจน่ะครับ ตอนนี้ในหัวโล่งไปหมด";
}
const customers = ["Somchai", "Arthit", "Suchada", "Napasorn", "Pimchanok"]
sortCustomerName(customers);
 // customers.sort((a, b) => a.localeCompare(b));
  // console.log(customers)

  // เรามี Array `customers` ซึ่งบรรจุรายชื่อสมาชิก ให้ใช้ Bubble Sort Algorithm เพื่อเรียงลำดับสมาชิกตามลำดับตัวอักษร 

  // เช่น ถ้า `customers = ["Somchai", "Arthit", "Suchada", "Napasorn", "Pimchanok"]` เมื่อ Algorithm นี้ทำงานจะแสดงผลลัพธ์ออกมาบนหน้าจอเป็น `[ "Arthit", "Napasorn", "Pimchanok", "Somchai", "Suchada"]`
  
  // ```jsx
  // function sortCustomerName(customers) {
  //   // เริ่มเขียนโค้ดตรงนี้จ้า
  // }
  
  // // ตอบคำถามตรงนี้จ้า
  // ```
  
  // ยังจำได้ไหมว่า Bubble Sort Algorithm มี Big O เป็นเท่าไร เพราะอะไร ให้เขียนอธิบายใน comment แนบมาข้างใต้โค้ดตรงส่วนที่ระบุว่า **“ตอบคำถามตรงนี้จ้า”**


// ตอบคำถามตรงนี้จ้า
